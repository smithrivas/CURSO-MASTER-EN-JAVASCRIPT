// Ejecuta el código, en caso de error se guarda en la variable que se declara en el Catch
try{
  var year = 2019;
  alert(year);
  var vector = new Array(99999999999999999);
  console.log(decodeURIComponent("www.you%$#tube.com"));
}catch(error){
  console.log(error);
  alert("A ocurrido un error en el código");
}
